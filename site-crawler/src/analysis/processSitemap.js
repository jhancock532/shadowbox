// Create a list of all valid site URLs found while crawling
let urls = pageData.map((page) => page.url);

// Download and parse the sitemap.xml file, if it exists, and compare the URLs found in the sitemap with the URLs found while crawling

// Create a summary of all the network request results for all the page types

{
}
